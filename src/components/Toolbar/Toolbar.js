import React from 'react'

import classes from './Toolbar.module.css'

const toolbar = () => {
    return (<div className={classes.Toolbar}>

        <div className={classes.ToolbarTitle}>Todo App</div>

    </div>)
}

export default toolbar;
